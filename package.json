{
  "name": "intelops-website",
  "version": "2.1.3",
  "license": "UNLICENSED",
  "author": "gethugothemes",
  "scripts": {
    "dev": "hugo server",
    "test": "hugo server --disableFastRender --templateMetrics --templateMetricsHints --watch --forceSyncStatic -e production --minify",
    "build": "hugo --gc --minify --templateMetrics --templateMetricsHints --forceSyncStatic",
    "update": "hugo mod clean && hugo mod get -u ./... && hugo mod tidy",
    "format": "prettier -w ."
  },
  "devDependencies": {
    "autoprefixer": "^10.4.17",
    "postcss": "^8.4.35",
    "postcss-cli": "^11.0.0",
    "prettier": "^3.1.0",
    "prettier-plugin-go-template": "^0.0.15"
  }
}
